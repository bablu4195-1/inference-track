# Copy to .env (gitignored) on the GPU host. Never commit the real token.
HF_TOKEN=hf_YOUR_READ_TOKEN_HERE
# Pinned for reproducibility (P15 records the digest actually used).
VLLM_IMAGE=vllm/vllm-openai:latest
MODEL_ID=Qwen/Qwen2.5-7B-Instruct
